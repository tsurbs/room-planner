/**
 * Optional: if you inject window.__RP_PARTYKIT_HOST__ before app.js, sync can read it
 * (index.html meta tag `room-planner-partykit` is preferred).
 * Copy this file to sync-config.js only if your deploy pipeline copies it into public/.
 */
// window.__RP_PARTYKIT_HOST__ = 'ws://127.0.0.1:1999';
